import { inter } from '@/app/ui/fonts';

export default function KnuwLogo() {
  return (
    <div
      className={`${inter.className} flex flex-row items-center leading-none text-black`}
    >
      <p className="text-[44px] font-bold">KNUW</p>
    </div>
  );
}
